# models/

Local model binary files (e.g. `.gguf`) go here when the Lab Runner
actually loads and runs one — not yet.

**Nothing in this directory except this README and `.gitignore` is ever
committed.** See `.gitignore` in this directory for the exact rule.

No model file exists here yet. No model has been downloaded or run.
