# RESEARCH-ONLY REPRESENTATION EXAMPLE -- NOT a RAW benchmark fixture,
# NOT part of Benchmark Profile v1, NOT consumed by any ModelAdapter or
# InferenceBackend, NOT wired into lab_runner's renderer/fixture_schema
# pipeline. See
# docs/2026-09-08-temporary-grounded-statements-design.md for the
# frozen design this shape implements -- not restated here.
#
# Case B -- an unresolved reporter. `who: null` represents that the
# deterministic system does not know who spoke -- per the frozen
# design's unresolved-speaker rule, this is never guessed and, per that
# same rule, this record's `who` is never later reassigned even if the
# speaker is subsequently identified elsewhere.
who: null
statement: "An unresolved speaker reported an intention to pick up Elijah."
reported_at: "2026-09-08T15:05:00Z"
source: "user_speech"
